package br.lois.algoritmos.search;

public interface SearchAlgorithm {
	
	/**
	 * Retorna a posição na array do valor desejado. Retorna -1 caso o elemento não for encontrado.
	 */
	int indexOf(int value);
	
}